# Editable install + pytest + ruff (see pyproject.toml [project.optional-dependencies].dev).
# From repository root:
#   bash scripts/setup-dev.sh
# or (if python3-venv is installed):
#   pip install -e ".[dev]"
-e .[dev]
